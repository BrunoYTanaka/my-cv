import { styled } from '@mui/material/styles'
import { motion } from 'framer-motion'

export const CircularSkillbar = styled('div')(() => ({
  width: 95,
  height: 95,
}))
export const CircularSkillbarProgess = styled(motion.div)(() => ({}))
